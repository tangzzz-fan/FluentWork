# FluentWork Prompt 工程与语料库设计文档

**版本**：V1.2　**日期**：2026年8月　**对应**：PRD V1.4 / 技术方案 V3.2　**定位**：重难点专项设计

> 本文档展开技术方案中两个"靠磨不靠写"的核心子系统：① Prompt 工程体系——产品反馈质量的唯一决定因素；② 语料库与调度引擎——留存资产的存储、组织与提取训练机制。

---

# 第一部分：Prompt 工程体系

## 1. 为什么 Prompt 工程是这个产品的命脉

FluentWork 的差异化不在"能对话"（ChatGPT 语音也能），而在**反馈的结构化质量**：评价是否引用了用户原句、话术块是否来自真实卡壳点、每日一读是否贴用户工作。这些 100% 由 Prompt 质量决定。因此 Prompt 不是"一段写在代码里的字符串"，而是需要版本化、可评估、可灰度的**核心资产**。

## 2. Prompt 资产管理架构

### 2.1 存储与发布

| 要求 | 方案 |
|---|---|
| 不发版可调 | 全部 Prompt 存配置中心（DB + 缓存），服务端运行时拉取，禁止硬编码 |
| 版本化 | 每个 Prompt 有版本号、变更说明、关联评估分；支持一键回滚 |
| 灰度能力 | 按用户哈希灰度新 Prompt 版本，对比评估分达标后全量 |
| 结构化输出 | 所有 LLM 调用强制 JSON Schema 输出，schema 校验失败自动重试一次，仍失败则丢弃并告警——脏数据永不进语料库 |

> **工程小传：什么是 JSON Schema 结构化输出**（面向非 LLM 背景读者）：LLM 默认返回自由文本，而"结构化输出"相当于跟模型约定"必须按指定字段格式返回"，类似给 LLM 定义 Codable 协议——不符合就解码失败。服务端因此能程序化校验每个字段（比如检查 `original_quote` 是否真是转录原文），而不是解析一段自由文本碰运气。

### 2.2 评估体系详设（W3 必须建好，先于此后所有调优）

> **工程小传：为什么要建评估集**：Prompt 每改一次，输出质量可能悄悄变差且肉眼难察（不像代码报错那样显性）。评估集就是"Prompt 的单元测试套件"：人工标注一批好坏样本，每次改 Prompt 跑一遍打分，分数下降禁止上线。类比：这就是给 Prompt 建 XCTest + CI 门禁。

#### 2.2.1 评估集构成（总量约 100 条，W3 冷启动 + 内测期补充）

| 任务类型 | 样本数 | 样本形态 | 覆盖重点 |
|---|---|---|---|
| 三层评价（C2/C3） | 30 | 完整会话转录 + 场景目标 | 典型卡壳、语法错、中式表达、低置信度 ASR（应舍弃不评）、目标达成/未达成各半 |
| 话术块炼化（D1） | 20 | 含卡壳点的会话转录 | 卡壳点命中/遗漏；反例：用户已说对的句子不得被提炼 |
| 每日一读（C5） | 15 | 话术块集合 + 素材主题 | 话术块自然融入度、体裁轮换、150-250 词约束、语料不足兜底话术 |
| 话题卡（H1） | 10 | 语料库快照 + 素材历史 | 话题与来源可追溯；反例：泛话题（如"聊聊周末"）必须判坏 |
| 语义判定（E2） | 25 | 中文意图 + 标准块 + 用户作答音频转录 | 语义等价但措辞不同（应判过）、语义偏差（应判败）、地道但不同的答法、中式英语口音导致的 ASR 错字（应意图还原） |
| 对话扮演（B2） | —— | 实时链路不做离线评估集 | 用内测期人工旁听 + 素材引用率埋点（≥60%）替代；V1.1 再建自动化评估 |
| 合计 | 100 | | 每类至少含 5 条"陷阱样本"（专门检测已知失效模式） |

#### 2.2.2 样本来源与冷启动（内测前无真实数据怎么办）

1. **内测前（W3）**：团队自造冷启动集——每人写 10-15 段代表性程序员对话转录（覆盖 standup 汇报、方案申辩、被质疑等场景），故意植入典型中式表达与卡壳；再由 AI 预标注 + 人工复核；
2. **内测期（W8 起）**：真实用户会话经授权后进入标注池，逐步替换自造样本（内测 4 周内真实样本占比达 50%）；
3. **反例构造**：向真实转录注入 ASR 错误（如同音词替换、吞词）构造低置信度样本，验证"意图还原"与"舍弃不评"两条规则。

#### 2.2.3 标注规范

- **标注粒度**：每条样本按对应任务的评分维度逐项打"达标/不达标"，并必须写一句不达标理由（理由本身是后续改 Prompt 的原料）；
- **双标 + 仲裁**：每样本至少 2 人独立标注，不一致时由产品负责人仲裁，仲裁结论写入《标注指南》作为后续判例；
- **标注人力**：由 AI 标注 Agent 预标注（给出初步结论 + 理由），人工只做复核与仲裁——这是超小团队能承担 100 条标注的前提（分工见《FluentWork团队分工文档》）。

#### 2.2.4 评分维度与权重（按任务类型）

| 任务 | 维度（权重） | 判定方式 |
|---|---|---|
| 三层评价 | 引用准确性（0.4）/ 可操作性（0.3）/ 相关性（0.2）/ 格式合规（0.1） | 引用准确性服务端自动校验（quote 不在转录中直接 0 分）；其余由 LLM 裁判初筛 + 人工抽检 |
| 炼化 | 卡壳点命中（0.5）/ 表达质量（0.3）/ 标签合规（0.2） | 标签不在预设枚举内自动 0 分 |
| 每日一读 | 语料融入自然度（0.4）/ 话题相关性（0.3）/ 约束合规（0.3） | 话术块使用数 <5 自动不合规 |
| 话题卡 | 来源可追溯（0.5）/ 实用性（0.3）/ 风格一致（0.2） | 无 source_refs 自动 0 分 |
| 语义判定 | 与人工标注一致率（单一指标） | 目标 ≥ 90% |

**LLM 裁判（LLM-as-judge）**：用一个强模型按上述维度给生成结果打分，作为人工标注的替代执行器；但裁判本身的可靠性先要用人工标注的 100 条验证（裁判与人工一致率 ≥ 85% 才允许用于回归）——裁判不可自证，必须先用人工基准校准。**裁判模型选型**：通常选比生产更强的旗舰模型，但成本随之上升——在 W3 评估集冷启动时一并验证两档候选模型（旗舰/次旗舰）的一致率与单轮回归成本，选定后写入配置；此项已列入 W3 验收范围。

#### 2.2.5 回归机制与发布门禁（CI 化）

```
Prompt 变更 → 提交配置中心新版本 → 自动触发评估集回归 → 生成 diff 报告
  ├─ 总分 ≥ 基线且无单项维度下降 > 5% → 允许灰度发布（按用户哈希）
  └─ 任一维度下降 > 5% 或引用准确性下降 → 禁止上线，一键回滚上一版本
```

- 回归结果与 Prompt 版本号绑定存档，每个版本可追溯"当时为什么能上线"；
- 灰度期对比新旧版本线上抽检通过率，达标后全量；
- 线上抽检：每周随机抽 20 条线上生成结果人工复核，抽检通过率纳入质量周报；连续两周低于 80% 触发 Prompt 专项优化。

#### 2.2.6 验收标准（W3 出口）

1. 冷启动评估集 ≥ 60 条完成双标 + 仲裁；
2. 回归脚本可一键执行并产出 diff 报告；
3. LLM 裁判与人工标注一致率验证完成；
4. 此后任何 Prompt 变更未经回归禁止进入配置中心。

## 3. 各模块 Prompt 设计要点与骨架

### 3.1 对话扮演（B2）——实时链路

角色：扮演用户的同事/Lead。核心难点是**贴素材而不读稿**。

骨架结构：

```
[角色设定] 你是用户的同事 {role}，正在参加 {scenario}
[素材注入] 讨论背景：{素材提炼结果——主题/术语/讨论点，≤500 token}
[行为约束] 
- 每次发言 ≤3 句，口语化，禁用书面长句
- 至少 60% 话轮必须引用素材中的具体内容提问或质疑
- 用户表达不清时追问澄清，不要替用户说完
[强化注入] 用户刚命中话术块 {chunk} 时，下一话轮自然肯定（"Good point, and I like how you put it"），不刻意、不重复句式
[禁止项] 不评价用户语法（那是回顾页的事）；不跳出角色
```

要点：素材注入必须用 A1 的**提炼结果**而非原文（上下文长度与聚焦度双重原因）；B7 命中通过增量 system 消息注入，不重建会话。

**素材引用率（≥60% 验收标准）的测量口径（V1.2 新增）**：对话扮演不做离线评估集，但验收标准必须可自动计算，否则内测期无法判达标。近似口径：

1. A1 提炼时同步产出 **素材关键词/术语 Top 10**（存 refine_json）；
2. 网关按会话统计"包含 ≥1 个关键词的 AI 话轮占比"，埋点入库（可自动计算的近似指标）；
3. 每周人工抽检 20 个 AI 话轮，校准近似口径与人工判断的一致性；偏差 >15% 时修订关键词提取策略（而非直接改 Prompt）；
4. V1.1 再建对话扮演的自动化评估（LLM 裁判判断"引用是否服务于提问/质疑"，而非纯字面命中）。

### 3.2 三层评价（C2/C3）——异步链路，质量敏感

输出 JSON Schema：`{goal_achievement, issues[], suggestions[], comparisons[]}`。

关键设计：

- **意图还原前置**：Prompt 中明确"ASR 转录可能有误，先结合对话上下文推断用户想表达的意思，评价基于意图而非字面转录；置信度不足的条目直接舍弃"；
- **强制引用**：`issues[]` 每项必须含 `original_quote` 字段且必须是转录中的原文子串——服务端做校验（quote 不在转录中则丢弃该条），这是"禁止泛泛而谈"的工程保证；
- **分层干预映射**：issue 带 `type` 字段（grammar / idiomatic / missing_info），客户端按类型决定呈现方式（自改标注 / 候选选择 / 骨架填充）；
- **数量纪律**：issues ≤5、suggestions ≤3、comparisons 3-8 条——宁可少而准，由 schema 的 maxItems 强约束。

### 3.3 话术块炼化（D1）

输出 Schema：`{blocks: [{intent_zh, expression_en, anchor_user_said, scene_tag, function_tag}]}`。

要点：

- **只从卡壳点和不地道表达提炼**，明确禁止提炼用户已说对的句子（Prompt 中写明选择标准 + 给 2 个反例）；
- `anchor_user_said` 必须是转录原文（服务端校验同上）；
- 标签从预设枚举中选（场景：standup/review/1on1/interview/casual；功能：object/clarify/report/propose/agree…），不允许自由生成——标签失控会毁掉语料库的筛选维度。

### 3.4 每日一读（C5）

输入：近 7 天话术块 + 最近素材主题。要点：

- 强约束"**必须自然使用以下话术块中的至少 5 个**"（列表注入），并在输出中标注使用了哪些——服务端校验，不满足则重新生成；
- 体裁轮换：standup 汇报 / 技术短文 / 对话片段，避免体裁单一导致疲劳；
- 150-250 词硬约束；语料不足时降级为预置场景模板生成，且**不标注"来自你的语料"**（诚实原则）。

### 3.5 话题卡（H1/H2）

输入：用户语料库中"已自动化"优先的话术块 + 素材主题历史。要点：

- 话题必须从素材主题与语料场景标签派生，输出含 `source_refs`（来源会话/素材 ID），无来源的话题不允许生成——这是"禁止泛话题"的工程保证；
- 每张卡的话术清单只引用该用户**自己库里的块**，开场句可以新造但风格对齐用户已练表达；
- 触发条件（话术块 ≥20）与生成频率（每周 3-5 张）均为服务端配置。

### 3.6 闪测语义判定（E2）

最小模型即可，但判定标准要写死：**语义等价 + 语法可接受即判成功，不要求逐字一致，允许不同但地道的表达**；输出 `{pass, reason}`，reason 一句话且必须具体（"意思正确，且 naturally 用了替换表达" / "语义偏差：把'推迟讨论'说成了'取消讨论'"）。发音评分（I3）随发音评测后置到 V1.1，MVP 的判定输出不含发音维度。

## 4. 成本与延迟的工程控制

- 异步任务（评价/炼化/每日一读/话题卡）批量合并调用：一次会话的评价+炼化合为一次 LLM 调用（一个 Prompt 输出合并 Schema），降本约 40%；
- 小模型优先：E2、B7 命中检测用最小模型；C/D/H 用旗舰；对话用旗舰流式；
- 每次调用记录 token 消耗，按用户/任务类型聚合，周报警示。

---

# 第二部分：语料库与调度引擎

## 5. 语料库的本质定位

语料库不是"收藏夹"，是**训练系统的弹药库 + 留存资产**。三个消费方：闪测调度（提取训练）、每日一读（内容生成原料）、话题卡（实战出口）。设计必须同时服务三者。

## 6. 数据模型详设

```
PhraseBlock {
  id, user_id,
  intent_zh,            -- 场景意图（中文）
  expression_en,        -- 英文话术块
  anchor_user_said,     -- 对比锚点（用户原句）
  scene_tag,            -- 场景枚举
  function_tag,         -- 功能枚举
  state,                -- new / training / automated
  success_streak,       -- 连续成功次数
  next_due_at,          -- 下次调度时间
  ease_factor,          -- 难度系数（调度用）
  real_use_count,       -- 实战命中次数（B7 回写）
  source_session_id,    -- 来源会话
  created_at, updated_at
}

DrillRecord {
  id, block_id, user_id,
  semantic_pass,        -- 语义判定结果
  pronunciation_score,  -- 发音分（字段预留 V1.1，仅记录，不影响调度）
  response_ms,          -- 提取耗时（疗效指标原料；起点：准备期结束、开始录音，与 UI 文档 4.4 对齐）
  created_at
}
```

索引设计：`(user_id, next_due_at)` 支撑调度取数；`(user_id, scene_tag)`、`(user_id, function_tag)` 支撑筛选；全文索引落在 `expression_en + intent_zh`（MySQL FULLTEXT 足够，量级人均数百条，不引入向量数据库）。

## 7. 调度引擎（提取训练的核心算法）

MVP 采用**简化 SM-2**（间隔重复经典算法的裁剪版），原则是"参数少、可解释、可调"：

| 事件 | 调度动作 |
|---|---|
| 新块入库 | state=new，next_due = 次日 |
| 提取成功（streak < 3） | streak+1，next_due = +1 天 / +3 天 |
| 提取成功（streak = 3） | state=automated，next_due = +7 天 |
| automated 复验成功 | next_due = +30 天，ease_factor 微调 |
| 提取失败（任何状态） | streak 清零，插入本轮尾部重练，next_due = 明天；automated 降级回 training |

关键设计决策：

1. **失败插队尾**（本轮内立即重现）比"明天再来"记忆固化效率高，这是与普通 Anki 类调度的差异点；
2. **发音分不进调度**（PRD 纪律：避免发音焦虑干扰提取目标），只做展示；MVP 期发音评测整体后置到 V1.1；
3. **实战命中回写**（B7）：话术块在真实对话中被用上，视同一次高强度成功——streak+1 且 ease_factor 上调。这把"练"与"用"在算法层闭环；
4. 所有间隔参数（1/3/7/30 天、streak 阈值）服务端可配，内测期重点调。

## 8. 疗效指标的计算口径

北极星次级指标"话术块自动化转化"依赖 DrillRecord 的可计算性：

- **转化率**：T 日前入库的块中，当前 state=automated 的比例（按周滚动）；
- **平均耗时**：入库时间 → 首次 automated 时间的均值；
- **提取速度**：response_ms 的分布变化（同一用户早期 vs 近期）——比"答对率"更能反映自动化程度，因为自动化的本质是**提取速度**而非正确率；
- **实战转化率**：real_use_count > 0 的 automated 块占比——衡量"练了真能用上"。

## 9. 隐私与删除的级联设计

素材删除 → 级联删除关联会话、话轮、录音、衍生话术块（用户可选保留话术块，但锚点脱敏为 "[已删除]"）；账号注销 → 全链路异步删除任务（MQ），每步记录回执，72 小时内完成并在设置页可见进度。语料库是用户最核心的资产，删除路径的可靠性与写入路径同等重要。

## 10. 演进路线

- **MVP**：上述简化 SM-2 + 三个消费方；
- **V1.5**：写模块题目从语料反向生成（第四个消费方）；换说法/概念封装训练复用同一调度引擎，仅扩展 DrillRecord 的 drill_type 字段；
- **V2.0**：团队场景下语料的脱敏聚合（如团队高频卡壳点榜单），需单独过隐私评审。

---

*评审重点：第 2 章评估体系详设（2.2.1-2.2.6，先建评估再调优的纪律与验收标准）、第 3.2 节强制引用校验、第 7 章调度算法与实战回写。评估集标注人力分工见《FluentWork团队分工文档》。*
