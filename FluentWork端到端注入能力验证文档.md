# FluentWork 端到端注入能力验证文档（POC）

**版本**：V1.1　**日期**：2026年8月　**对应**：技术方案 V3.2 第 3.3 节　**执行窗口**：W1-W2　**性质**：Go/No-Go 前置验证

> 本文档解决技术方案中已登记的最高优先级未验证假设（风险登记册 #1.5）：**火山引擎端到端实时语音形态是否支持 B7 命中检测所需的两项能力——① 获取用户话轮的 ASR 文本；② 会话中途注入文本指令**。验证不通过则"说的房间"必须改走模块化编排，1.5 秒首响红线需要重测，此决策影响 W3 之后全部排期，因此必须在 W1-W2 内出结论。

---

## 一、背景：为什么这是一个未验证假设

B7（对话中命中已练话术块的即时反馈）是产品差异化的核心（PRD 7.2），其技术路径（技术方案 3.3）要求两件事：

```
用户说完一句话
  ├─ 需要①：拿到这句话的 ASR 文本 → 旁路小模型做话术块语义匹配
  └─ 命中后需要②：向下一轮 AI 回复注入 system 增量（"用户刚用了 XX 表达，自然确认"）
```

而技术方案 2.2 为延迟最优选择了**端到端实时语音大模型**——它的卖点是"一个连接搞定 ASR+LLM+TTS"，本质是黑盒。**黑盒可能不暴露中间文本，也可能不允许会话中途修改指令**。两者只要缺一个，B7 的既定路径就不成立。

判断标准重申（技术方案 2.2）：凡需要"中间结果"的场景走模块化编排。说的房间本不需要中间结果——**是 B7 让它变成了需要中间结果的场景**，这正是必须实测而不能拍板的原因。

---

## 二、验证项清单与通过标准

| 编号 | 验证项 | 通过标准 | 结果档位 |
|---|---|---|---|
| V1 | 端到端会话能否**返回用户话轮的 ASR 文本**（事件回调/流式文本） | 每个用户话轮结束后 ≤500ms 内可拿到文本，且可用于旁路匹配 | 必需 |
| V2 | 会话中途能否**注入/更新文本指令**（如 session 级 system 更新、上下文追加） | 存在官方支持的会话中注入通道（非重建会话） | 必需 |
| V3 | 注入的**生效时机**：本轮注入能否影响"下一句"AI 回复 | 用户话音结束后 800ms 内完成的注入，能体现在 AI 下一话轮行为中 | 必需 |
| V4 | 注入对**主链路延迟**的影响 | 开启注入通道后，首响 P90 劣化 ≤ 100ms（红线仍为 1.5s） | 必需 |
| V5 | 注入行为的**可控性**：能否验证 AI 确实"自然确认"了命中表达 | 构造 10 次命中注入，AI 话轮出现确认行为的比率 ≥ 70%，且不机械重复句式 | 必需 |
| V6 | **旁路混合形态**可行性：端到端继续承担对话，旁路另开一条模块化流式 ASR 只取用户文本 | 旁路 ASR 文本与端到端识别一致率 ≥ 90%；不干扰主链路 | 备选（V1 不通过时） |
| V7 | **模块化编排替代链路**基线延迟 | 流式 ASR + LLM + 流式 TTS 拼装的首响 P90 实测值（用于决策对比，无预设门槛） | 必测 |
| V8 | **有效注入窗口**：从 VAD 判停到"注入仍能影响当轮回复"的实际时长 | 分档延迟注入（≥30 轮）测出窗口分布，记录模型开始生成的时刻；输出窗口 P50/P90 | 必需（V3.1 审核新增） |

**逻辑关系**：V1+V2+V3+V4+V5 全通过 → 结论 A（端到端主链路，最优）；V1 不通过但 V6 通过且 V2-V5 通过 → 结论 B（端到端 + 旁路 ASR 混合）；V2/V3 不通过 → 结论 C（说的房间改走模块化编排，按 V7 数据重估首响红线）。**V8 不改变 A/B/C 结论，但决定 B7 落入技术方案 3.3 的哪个退路档位**：窗口 ≥800ms → 档位①当轮确认；窗口不足 → 档位②下轮开局确认；注入不可用 → 档位③仅徽章。此决策在 POC 阶段完成，不等压测报告。

---

## 三、测试用例

| 用例 | 前置 | 操作 | 预期 / 记录项 |
|---|---|---|---|
| T1 基础对话 | 建立端到端会话，注入 standup 素材上下文 | 完成 8 回合自然对话 | 首响延迟逐回合记录（端上打点为准，技术方案坑清单 #8） |
| T2 文本获取 | T1 会话中 | 用户说话结束，监听所有回调事件 | 记录是否存在话轮文本事件、到达延迟、字段格式 |
| T3 中途注入 | 会话进行中 | 调用文档中一切可能的"更新指令/追加上下文"入口 | 记录：是否有官方入口；无入口时记录工单确认结果 |
| T4 注入生效 | T3 成功 | 注入"下一句请自然肯定用户提到的 cache invalidation 表达"，观察 AI 下一话轮 | 行为命中计数（10 次取比率） |
| T5 延迟对照 | 同一素材 | 同一对话脚本分别在"无注入通道 / 开启注入通道"下各跑 20 轮 | 两组首响分布对比，P90 差值 |
| T6 旁路 ASR | 端到端会话同时开一条模块化流式 ASR（同一麦克风流的镜像） | 对比同一话轮两路识别结果 | 一致率；旁路是否引入端上性能问题（CPU/电量抽查） |
| T7 模块化基线 | 纯模块化编排会话 | 同一对话脚本跑 20 轮 | 首响分布（分段：ASR 尾包 → LLM 首 token → TTS 首帧） |
| T8 打断兼容 | 注入通道开启状态 | 用户在 AI 说话中途打断 | 打断响应 ≤200ms 不受注入通道影响；打断后注入状态不残留到错误话轮 |
| T9 注入窗口 | T3 成功 | 用户话音结束后按梯度延迟（200/400/600/800/1000ms）注入"请确认用户提到的 XX 表达"，各 ≥6 轮，观察当轮回复是否出现确认行为 | 窗口分布（仍生效的最大延迟）＋ 模型开始生成时刻的记录方式（首音频帧时间戳近似） |

**样本要求**：全部用例使用**中式口音英语**语料（团队成员自录，覆盖典型发音问题），因为 W1-W2 压测同时承担 WER 验证；每用例至少 3 台设备（外放 + 耳机两种音频路径）。

---

## 四、延迟预算分解（结论 A 情景）

```
用户话音结束
  ├─ 端到端主链路：VAD 判停 → 模型生成 → TTS 首帧 …… 预算 ≤ 1.5s（红线）
  └─ 旁路（并行，不阻塞）：
       话轮文本到达（≤500ms）→ 命中匹配小模型（≤300ms）→ 注入下发
       合计 ≤ 800ms —— 必须赶在 AI 开始生成下一话轮之前（技术方案 3.3 硬约束）
```

若实测"话轮文本到达 + 匹配"常态 >800ms，或 T9 测得的有效窗口 < 旁路总耗时，则按技术方案 3.3 退路分级落入档位②（下轮开局确认）或档位③（仅徽章）——两档的产品形态已预定义，无需再拍方案；档位分布记入验证报告。

---

## 五、决策矩阵与下游影响

| 结论 | 条件 | 说的房间链路 | B7 形态 | 下游影响 |
|---|---|---|---|---|
| A 最优 | V1-V5 全通过 | 端到端 | 完整形态（当轮确认） | 无排期影响 |
| B 混合 | V1 失败、V6+V2-V5 通过 | 端到端 + 旁路流式 ASR | 完整形态；端上多一路音频镜像 | 端上音频引擎增加分流工作，评估 ≤2 天增量 |
| C 切换 | V2 或 V3 失败 | 模块化编排（按 2.2 判断标准本就合理） | 完整形态；延迟以 V7 实测为准 | **若 V7 首响 P90 > 1.5s：启动降级评审**（分段 TTS、更小的对话模型），排期重估并同步 PRD 体验红线 |

无论 A/B，均按 V8（T9）窗口数据确定 B7 档位（①/②/③），并把档位埋点写进 W3 的会话埋点需求。

**无论哪个结论**：网关防腐层的价值被再次确认——切换只发生在网关内，客户端无感（技术方案 2.3）。

---

## 六、执行计划（嵌入 W1-W2）

| 天 | 事项 | 产出 |
|---|---|---|
| D1 | 开通火山端到端与模块化两套试用额度；调研 Agent 输出文档先验报告（《团队分工文档》第三章） | 先验结论备忘（仅作参考） |
| D2 | 打通端到端最小会话（网关直连测试工具，不经客户端） | T1 可执行 |
| D3-D4 | 执行 T2/T3/T4（能力探测为主，遇文档不明立即提工单 + 联系技术支持） | V1-V3、V5 结论 |
| D5 | T5 延迟对照 + T8 打断兼容 + **T9 注入窗口** | V4、V8 结论；B7 档位确定 |
| D6 | T6 旁路 ASR 验证；开通账号时同步完成免费额度算账（技术方案 2.1） | V6 结论 |
| D7 | T7 模块化基线（与主压测共用用例，一次跑双份数据） | V7 数据 |
| D8 | 汇总验证报告 → **Go/No-Go 评审输入件** | 验证报告（模板见附录） |

**外部依赖**：火山技术支持工单响应可能慢于预期——D3 遇阻立即升级到商务对接人，不在工单上死等；若 D5 结束仍无 V2 结论，默认按结论 C 准备（不等供应商答复阻塞排期）。

---

## 附录：验证报告模板

```
1. 结论档位：A / B / C
2. 各验证项实测数据表（V1-V7，含原始延迟序列）
3. 关键证据：事件回调日志片段 / 注入 API 请求响应 / 工单编号与答复原文
4. 若结论 C：模块化链路首响分布、降级方案建议、排期影响评估
5. 遗留问题清单（进坑清单跟踪）
签字：技术负责人 / 日期
```

---

*评审重点：第二章通过标准与结论逻辑关系、第五章决策矩阵（结论 C 的降级触发条件）、第六章"不被供应商答复阻塞"的兜底纪律。*
