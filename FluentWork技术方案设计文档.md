# FluentWork 技术方案设计文档

**版本**：V3.3　**日期**：2026年8月　**对应 PRD**：V1.4　**评审角色**：技术负责人 / 架构师

> V2.0：语音链路定案火山引擎实时语音方案。V3.0：**客户端技术转型——放弃 Flutter 跨端，改为 iOS SwiftUI 原生优先**；新增转型决策论证与三方库选型；Prompt 工程与语料库拆分为独立专项文档（《FluentWork-Prompt工程与语料库设计文档》），本文保留接口衔接说明。V3.1：与 PRD V1.3 对齐——最低系统版本定为 iOS 17+（消除 NavigationStack/@Observable/SwiftData 的版本矛盾）；发音评测与订阅内购后置到 V1.1；新增端到端注入能力验证（B7 依赖项，见《FluentWork端到端注入能力验证文档》）。**V3.3：iOS 状态管理定案轻量 Redux（TGReduxKit 2.0.0）+ Factory 依赖注入（ADR-001，详见《FluentWork-iOS App端技术设计文档》1.4），三方库白名单同步更新。** 本文档以企业实际落地为出发点。

---

## 一、技术挑战总览

对 PRD 全量功能做技术复杂度评估，难度分布极度不均：

| 模块 | 技术难度 | 本质 |
|---|---|---|
| 实时语音对话（B1/B7） | ★★★★★ | 全项目唯一高难度模块，体验生死线；B7 依赖端到端注入能力，W1-W2 必须验证（见专项文档） |
| 发音评测（I1，后置 V1.1） | ★★★★ | 接入成熟能力不难，评分校准难；MVP 不实施 |
| AI 内容生成质量（C2/C3/D1/H1） | ★★★☆ | Prompt 工程 + 评估体系，靠"磨"不靠"写" |
| 客户端音频工程 | ★★★☆ | 音频会话、打断、后台播放，琐碎但坑多 |
| 其余（账号/语料库/订阅/闪测调度） | ★★ | 常规工程，无技术风险 |

**第一条技术纪律：资源向语音链路倾斜，其余模块一律采用最朴素成熟的方案。**

---

## 二、语音链路定案：火山引擎实时语音方案

### 2.1 选型结论

语音链路采用**火山引擎实时语音方案**作为 MVP 唯一供应商，理由（面向国内用户的硬约束）：

1. **合规**：国内服务商，用户语音与素材数据不出境，满足 PRD 隐私承诺的落地前提；
2. **延迟**：国内节点网络链路最短，端到端延迟优化由厂商完成，1.5s 红线的达成概率最高；
3. **成本验证**：实时语音大模型、流式 ASR、TTS 均有免费试用额度——但额度通常按月/按量限额，"够覆盖内测"是未验证假设；**W0 必须算账**：人均日会话分钟数 × 20 人 × 4 周 vs 免费额度，结果写进供应商确认记录，不够在 W1-W2 压测窗口同步谈加量；
4. **中文/中式口音适配**：ASR 对中式英语的鲁棒性优于海外通用服务。

### 2.2 接入形态：两种模式的判断

火山引擎实时语音提供两种接入形态，按场景选择：

| 形态 | 说明 | 本项目用法 |
|---|---|---|
| 端到端实时语音大模型 | 一个连接搞定 ASR+LLM+TTS，延迟最优 | **说的房间（B1）主链路** |
| 模块化编排（流式 ASR + LLM + 流式 TTS 分别调用） | 每一环独立可控 | **闪测判定（E1/E2）与每日一读跟读（I2）**：这些场景需要拿到中间产物（ASR 文本做语义判定、用户录音做发音评测），黑盒端到端拿不到 |

**判断标准：凡是需要"中间结果"的场景（要拿用户说了什么文本、要拿原始录音做评测），走模块化编排；只追求对话流畅的场景，走端到端。**

### 2.3 接入架构

```
┌────────────────────────────────────────────┐
│  iOS App（SwiftUI + 原生音频引擎层）        │
└──────────────┬─────────────────────────────┘
               │ WSS（自有协议，Opus 音频帧 + JSON 控制帧）
┌──────────────▼─────────────────────────────┐
│  语音会话网关（自研，防腐层）                  │
│  ├─ 协议适配：自有协议 ⇄ 火山引擎 API          │
│  ├─ 会话状态机                                │
│  ├─ 旁路任务：B7 命中检测 / 成本埋点           │
│  └─ 降级切换：端到端 ⇄ 模块化 ⇄ 纯文本        │
└──────────────┬─────────────────────────────┘
               │
        火山引擎（端到端实时语音 / ASR / TTS）
```

**防腐层是必须做的，不是因为要换供应商，而是因为接入后必然要做三件事**：① 计费与配额控制在服务端（API Key 永不下发客户端）；② B7 命中检测等定制逻辑要挂在链路旁路上；③ 供应商接口升级或故障时，切换只在网关内发生，客户端无感。

### 2.4 自有协议设计（客户端 ⇄ 网关）

WebSocket，二进制音频帧 + JSON 控制帧：

- 控制帧：`session.start`（携带素材上下文/场景/音色配置）、`user.speech.start/end`、`ai.text.delta`、`ai.audio.chunk`、`interrupt`、`feedback.badge`（B7 命中通知）、`session.end`；
- 音频帧：Opus 编码，20ms 帧；
- 序列号机制：所有 AI 音频帧带递增序列号，打断时客户端丢弃序号早于打断点的帧（解决"打断时 TTS 流仍在下发"的竞态）。

---

## 三、自研侧必须自己把控的点

接入火山引擎后，以下能力**供应商不替你解决，是产品体验的差异化所在，必须自研自控**：

### 3.1 VAD 与话轮策略

端到端模式自带 VAD，但默认参数是通用场景，**程序员技术对话的停顿模式不同**（说技术名词前常有思考停顿）。把控点：

- 静音判停阈值放服务端配置，默认 300ms 起步，内测期按用户停顿分布调整，对"长停顿习惯"用户放宽到 500-800ms；
- 保留"按住说话"模式作为兜底：自动判停失效的场景（嘈杂环境、用户习惯）可手动控制起止，设置页可切换；
- 内测期专门采集"被切断/等太久"两类 badcase，这是调参依据。

### 3.2 打断（barge-in）状态机

端到端 API 提供打断能力，但**端上体验要自己保证**：检测到用户语音能量超阈值 → 本地立即停播（不等服务端确认）→ 上行 interrupt → 丢弃过期序列号音频帧。打断响应的目标是 200ms 内声音消失，这个指标只有端上做得到。

### 3.3 B7 命中检测（产品差异化核心）

完全自研的旁路服务：用户话轮 ASR 文本 → 并行调用小模型做"已入库话术块集合内语义匹配" → 命中结果两件事：① 注入下一 AI 话轮的 system 增量（让 AI 自然确认）；② 网关推 `feedback.badge` 给客户端。**硬约束：全程旁路，800ms 超时或失败直接跳过本轮反馈，绝不影响主链路延迟。**

**前置依赖（V3.1 新增）：上述①依赖端到端形态支持会话中途文本注入，此为未验证假设，必须在 W1-W2 完成《FluentWork端到端注入能力验证文档》定义的 POC 后再定 B7 技术路径。**

**注入时序退路分级（V3.2 新增，不等压测报告再拍产品方案）**：端到端模型在 VAD 判停后几乎立即开始生成回复，LLM 上下文很可能几百毫秒内锁定——旁路 800ms 的注入在多数情况下只能影响"下下轮"。因此 B7 预设三档形态，POC 测出"有效注入窗口"实际长度后直接对号入座：
- 档位 ①（窗口 ≥ 800ms）：当轮确认，理想形态；
- 档位 ②（窗口不足）：下轮开局确认——第 N 轮命中未赶上，第 N+1 轮开场自然带出确认；
- 档位 ③（注入不可用）：仅徽章不注入——产品侧感知不减（徽章 + 实战使用次数回写），只损失 AI 口头确认。
内测期埋点记录命中实际落入的档位分布，作为体验调优依据。

### 3.4 Prompt 资产与生成质量

对话角色扮演（B2）、三层评价（C2/C3）、话术块炼化（D1）、每日一读（C5）、话题卡（H1）的全部 Prompt 自研，且必须工程化管理：

- Prompt 存配置中心，版本化、可灰度，不发版可调；
- 全部 LLM 调用强制 JSON Schema 结构化输出，解析失败不进库；
- **W3 前建好离线评估集**（内测标注 50-100 条好/坏反馈样本），每次改 Prompt 跑回归；
- 低置信度条目丢弃（C3 意图还原的硬规则）。

### 3.5 间隔重复调度引擎

闪测调度（E3）是纯自研规则引擎：新块次日重现、失败块插队尾、连续 3 次成功转已自动化、7/30 天间隔。所有参数服务端可配。这是留存的核心机制，不外包给任何通用逻辑。

### 3.6 发音评测的校准层（V1.1 实施）

发音评测接商业 API（火山或讯飞），但**原始打分分布偏宽松，必须自研校准映射**：原始分 → 可懂度三档（清晰/可懂/需改进）。校准参数用内测期人工标注数据拟合，放服务端配置。PRD 定调"可懂度优先"，产品层每次最多呈现 3 个发音问题。**节奏（V3.1）：MVP 不实施；内测期同步采集人工标注数据，随 V1.1 上线。**

---

## 四、接入火山引擎后，原方案的调整清单

| 原方案项 | 调整 |
|---|---|
| 语音供应商 | 海外实时 API → 火山引擎（端到端为主、模块化为辅） |
| 合规架构 | 删除"数据出境评估"事项；补充火山引擎企业协议中"API 数据不用于训练"条款的确认（采购谈判明确） |
| 成本模型 | MVP 内测期语音成本趋近于零（试用额度覆盖）；成本埋点仍 W1 上线，额度用尽前完成付费转化验证；正式期按火山计费重新测算免费额度上限 |
| 闪测/跟读链路 | 明确走模块化编排（需中间产物），不接端到端 |
| W1-W2 压测内容 | 从"验证自建链路延迟"改为"验证火山端到端在国内网络下的首响 P90 + 中式口音 WER + 外放 AEC 效果"，并新增**端到端注入能力验证**（B7 依赖，见专项验证文档）；Go/No-Go 标准不变 |
| 音色与口音 | MVP 用火山默认音色；多口音（印度/欧洲口音）需求整体后置到 V1.5 听力训练，选型时提前确认供应商多口音支持度 |
| 降级链路 | 三级降级：端到端故障 → 模块化编排 → 纯文本对话；降级切换逻辑在网关内实现 |
| 采购事项 | 并发配额写进合同（长连接瓶颈在供应商配额而非自家服务）；确认免费额度到期时间与正式计费阶梯 |

---

## 五、接入注意事项（坑清单）

1. **并发配额**：实时语音按并发路数计费/限流，内测 20 人没问题，但要确认突发扩容的响应时间；网关侧做排队与超限友好提示，不让用户看到裸错误。
2. **会话上下文注入**：`session.start` 携带的素材上下文有长度上限，素材提炼结果（A1）要做压缩，控制在供应商限制内——这也是 A1 必须"先提炼再注入"而非原文直传的技术原因。
3. **音频格式对齐**：端上 Opus 编码参数、采样率必须与供应商要求严格一致，参数不匹配的典型症状是"偶发识别错乱"而非报错，排查成本高，接入第一天就做端到端音频回环自测。
4. **打断后的上下文一致性**：打断发生时，被丢弃的 AI 半句话不要进入对话历史，否则下一轮 LLM 会基于"自己没说完整的话"继续，产生逻辑断层。网关负责维护一致的话轮历史。
5. **TTS 音色与语速**：语速可调（每日一读 0.8x/1.0x/1.2x 需求），接入时确认流式模式下变速是端上做还是云端做，避免重复实现。
6. **鉴权时效**：火山 API 临时令牌有过期机制，网关负责刷新，长会话（>30 分钟）不断线。
7. **审计与留存**：录音与转录的服务端留存周期与火山侧存储策略对齐，用户一键删除时要同时清理供应商侧关联数据（如适用），删除回执记录进 MQ 任务。
8. **监控口径**：延迟埋点按"端上打点为准"（用户真实感知），供应商控制台数据只做对账，不作为验收依据。

---

## 六、客户端技术方案：iOS SwiftUI 原生（V3.0 转型）

### 6.1 转型决策：为什么放弃 Flutter

| 考量 | 分析 |
|---|---|
| 音频工程 | 本产品 80% 的体验在音频链路（低延迟采集/播放、AEC、打断、后台播放）。Flutter 下这层本来就必须原生实现（V2.0 已定），桥接层只增加调试复杂度和延迟不确定性——跨端的收益被架空 |
| 平台保真度 | UI/UX 文档定义的语音状态机动效、徽章弹性动画、波形随音量起伏，原生 SwiftUI + Core Animation 的实现成本与效果均优于 Flutter 自定义渲染 |
| 系统能力 | StoreKit 2、APNs、AVAudioSession、CallKit 级打断处理，原生接入最直接 |
| 团队成本 | 单人/小团队维护一套原生 iOS 的成本，低于维护"Flutter UI + 双端原生音频桥"的混合栈 |

**代价与决策**：放弃 Android 并行。MVP 目标用户（外企/互联网公司程序员）iOS 占比高，且产品假设验证不依赖平台覆盖。Android 端在 V2.0 视 iOS 留存数据决定是否启动，启动时技术栈选原生 Kotlin（不回头用 Flutter——音频层的原生工作无法复用给跨端，UI 层反而是好重写的部分）。

### 6.2 iOS 技术栈（最低支持 iOS 17+，V3.1 定案）

> 版本决策说明：早期方案写 iOS 15+，但 NavigationStack（iOS 16+）、@Observable（iOS 17+）、SwiftData（iOS 17+）三处互相打架，兼容器旧版本意味着双轨维护（ObservableObject 兜底 + GRDB 兼容层）。目标用户群（互联网/外企程序员）设备更新率高，2026 年 iOS 17+ 设备占比已足够，直接定为最低版本，换取技术栈单一。

- **UI**：SwiftUI，导航用 NavigationStack；"说的房间"等沉浸页用全屏 cover；
- **响应式与并发**：不引入 Combine，统一 Swift Concurrency（async/await + Actor）；状态管理用**轻量 Redux 单向数据流**（TGReduxKit，基于 @Observable 的 iOS 17 原生适配）：单一根 Store + ScopedStore；依赖注入用 Factory（见 6.3）；
- **音频引擎**：AVAudioEngine 采集 + AVAudioPlayerNode 流式播放；`AVAudioSession` 设 `.voiceChat` 模式启用硬件 AEC 与回声消除（Voice Processing I/O）；打断逻辑在音频引擎层实现（本地 200ms 停播）；
- **网络**：业务接口用 `URLSession` + async/await；语音 WebSocket 用原生 `URLSessionWebSocketTask`（稳定，不引入三方 WS 库）；
- **Opus 编解码**：火山引擎 iOS SDK 自带的音频编解码优先；若需自行编码，SPM 接入 libopus 封装；
- **本地存储**：SwiftData 缓存语料库/历史/每日一读（iOS 17 统一定案，不再保留 GRDB 兼容轨）；读本地优先 + 后台同步；录音上传后 7 天本地清理；
- **内购**：StoreKit 2（原生），交易凭据服务端校验；`Transaction.currentEntitlements` 管理订阅状态（V1.1 实施，MVP 预留接口）；
- **推送**：APNs + UNUserNotificationCenter，闪测复习/每日一读/话题建议三类通知独立开关；
- **图表**：进步曲线用 Swift Charts（原生）。

### 6.3 三方库清单（克制原则：能原生不三方）

| 库 | 用途 | 选择理由 |
|---|---|---|
| 火山引擎 iOS SDK | 实时语音/TTS/ASR 接入 | 供应商官方，音频链路质量保障 |
| TGReduxKit（exact 2.0.0） | 全局状态管理（Redux 单向数据流） | 与 iOS 17+ / @Observable / Swift Concurrency 完全同栈；比 TCA 轻；选型对比见 iOS 端详设 1.4 |
| Factory（exact 3.3.2） | 依赖注入容器 | 轻量、SwiftUI/@Observable 友好、测试可重置容器；统一注册 Service 与 Middleware 工厂 |
| SwiftLint | 代码规范静态检查 | 团队规范统一，CI 集成 |
| Pulse（可选） | 网络请求调试日志 | 内测期排查音频链路问题，Release 移除 |

明确不引入：Alamofire（URLSession 足够）、RxSwift/Combine 三方扩展（Swift Concurrency 统一）、GRDB（iOS 17+ 定案后统一用 SwiftData）、Lottie（动效用 SwiftUI 原生动画，PRD 纪律禁止花哨动效）、Firebase 全家桶（自建埋点 + 国内合规考虑）、TCA（框架锁定与心智负担，ADR-001 排除）。

### 6.4 客户端架构

MV + 轻量 Service 层：

```
Views (SwiftUI)
  └─ ScopedStore（TGReduxKit 单向数据流，见 iOS 端详设 1.4）
       ├─ Middleware（副作用，依赖由 Factory 注入）
       └─ Service 层（APIClient / AudioEngine / SpeechSession / CorpusStore）
            └─ 语音会话网关（WSS）/ 业务 API（HTTPS）/ SwiftData
```

- `SpeechSession`：封装语音状态机（等待/录音/处理/播放/打断），对 View 只暴露状态流，音频细节不外泄；
- `AudioEngine`：采集、播放、AEC、打断、后台播放配置，独立单测可覆盖；
- 音频引擎与状态机是仅有的两个需要重度单测的客户端模块，其余 UI 层以快照测试抽查；
- 模块内部详设（状态机事件表、打断时序、序列号丢帧、Agent 任务拆解）见《FluentWork-iOS App端技术设计文档》。

### 6.5 服务端与其他部分（不变，简述）

- **服务端**：模块化单体 + 独立语音会话网关；MySQL + Redis + 对象存储 + MQ；不上微服务、不上 K8s；
- **Prompt 工程与语料库/调度引擎**：见专项文档《FluentWork-Prompt工程与语料库设计文档》；
- **隐私**：字段级加密、级联删除回执、供应商数据条款确认——同 V2.0。

---

## 七、技术风险登记册（V2.0 更新）

| # | 风险 | 概率 | 影响 | 对策 |
|---|---|---|---|---|
| 1 | 火山端到端延迟/口音 WER 不达标 | 低-中 | 高 | W1-W2 压测 Go/No-Go；不达标切模块化编排形态重测 |
| 1.5 | 端到端形态不支持话轮级文本注入，B7 无法落地 | 中 | 高 | W1-W2 按《FluentWork端到端注入能力验证文档》执行 POC；不支持则说的房间改走模块化编排并重测延迟红线 |
| 2 | 供应商并发配额成为增长瓶颈 | 中 | 中 | 采购合同锁配额；网关排队与超限提示 |
| 3 | 原生转型后 iOS 人力不足导致排期滑移 | 中 | 中 | UI 层拥抱 SwiftUI 声明式开发提速；音频引擎 POC 前置到 W1；Android 明确不并行 |
| 4 | 外放回声污染识别 | 高 | 中 | `.voiceChat` 模式强制硬件 AEC；内测必测外放 |
| 5 | AI 生成内容质量不稳定 | 高 | 中 | 离线评估集回归；低置信度丢弃；人工抽检 |
| 6 | 供应商接口变更/故障 | 低 | 高 | 防腐层协议隔离；三级降级链路（端到端→模块化→文本） |
| 7 | 免费额度耗尽后单位经济模型不成立 | 未知 | 高 | 成本埋点 W1 上线；内测期跟踪转付费信号；V1.5 评估开源自托管降本（FunASR + CosyVoice 2 + Pipecat/LiveKit 编排，网关内切换，客户端无感） |
| 8 | 内购审核延误（V1.1 阶段） | 中 | 中 | V1.1 启动时提前提审；订阅入口远程开关 |

---

## 八、8 周技术排期（V2.0）

| 周期 | 技术任务 | 出口标准 |
|---|---|---|
| W1-W2 | 语音会话网关 + 火山端到端打通；iOS 音频引擎 POC（AVAudioEngine + AEC + 打断）；压测报告（延迟/WER/回声）；**端到端注入能力验证（B7 依赖，见专项验证文档）** | **Go/No-Go 评审**：首响 P90 ≤ 1.5s 且中式口音 WER 达标，且 B7 注入路径已验证（或确认切模块化编排） |
| W3-W4 | 说的房间全量（SwiftUI + SpeechSession 状态机/打断/转录浮层）；业务服务骨架；Prompt 评估集建立 | 完整对话链路可用 |
| W5 | 异步任务管线（评价/炼化/B7 命中检测）；回顾页接口 | 说 → 读 → 入库打通 |
| W6 | 语料库/每日一读（模块化编排接入）；提审材料启动（订阅内购后置到 V1.1，沙盒验证同步后移） | 语料库与每日一读全流程通过 |
| W7 | 闪测调度引擎（模块化 ASR + 语义判定）；话题卡生成；APNs 推送；**周末提交 feature-complete 构建提审 TestFlight** | 完整闭环；提审已提交 |
| W8 | 审核问题修复、稳定性加固、压测、TestFlight 内测发布 | 20 人内测跑通 |

---

## 九、技术纪律（不做的事）

1. MVP 期不自研/自托管 ASR、TTS——V1.5 降本期再评估开源自托管，且只在网关内切换；发音评测同样不自研，且整体后置到 V1.1；
2. 不上微服务、不上 K8s；
3. 不做会议级音视频基建——这是异步可容忍的训练产品；
4. MVP 不做多口音 TTS（后置 V1.5 听力训练）；
5. 不引入向量数据库——人均数百条语料，MySQL 全文 + 标签足够；
6. **MVP 不做 Android 端**——iOS 原生（iOS 17+）先行验证产品假设，Android 视留存数据在 V2.0 决策（原生 Kotlin，不回头用跨端框架）；
7. 客户端能原生不三方——三方库白名单制（见 6.3），新增依赖需评审；
8. **MVP 不做发音评测与订阅内购**——发音评测的校准数据依赖内测标注，订阅需先验证核心价值，两者均在 MVP 验证后随 V1.1 启动，8 周排期全力聚焦"说 → 读 → 炼化 → 闪测 → 语料库"闭环。

---

*评审重点：第二章接入形态判断（2.2）、第三章自研把控点（3.3 的注入验证前置依赖）、第五章坑清单、第六章客户端转型决策与三方库白名单。Prompt 工程与语料库的深度设计见专项文档；B7 注入能力验证见《FluentWork端到端注入能力验证文档》。*
