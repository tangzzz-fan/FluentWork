# FluentWork 团队分工文档（人类 × AI Agent 协作）

**版本**：V1.1　**日期**：2026年8月　**对应**：PRD V1.4 / 技术方案 V3.2 / 后端方案 V1.2　**定位**：组织与执行设计

> 本文档回答一个问题：在一个超小人类团队（1-2 人）的前提下，如何借助 AI Agent 完成 8 周 MVP 的交付。核心思想：**人类做决策与把关，Agent 做产出与回归**；每一个 Agent 的产出都必须有明确的人类验收门禁，没有门禁的 Agent 产出不允许进入主干。

---

## 一、分工前提与设计原则

1. **人力现实**：人类团队为 1 名产品/技术负责人（iOS 主力）+ 0.5-1 名后端工程支持。这个配置按传统分工不可能在 8 周内交付全量 MVP——因此 AI Agent 不是"辅助工具"，而是**正式产能**，需要像管理外包团队一样管理它们：明确任务书、验收标准、质量门禁。
2. **门禁原则**：Agent 产出 = 草稿，人类评审 = 合入。任何 Agent 生成的代码、Prompt、标注，未经人类确认不得进入主干/配置中心/评估集。
3. **单点风险对冲**：人类是单点，因此所有决策必须文档化（本套文档即是），Agent 的上下文依赖项目文档而非个人口头知识——文档库本身就是 Agent 的"入职手册"。
4. **保密边界**：内测期真实用户数据（素材、转录、录音）不投喂给任何第三方 Agent 服务；Agent 只接触脱敏/自造数据与代码本身。

---

## 二、人类角色与职责（不可委托清单）

### 2.1 产品/技术负责人（1 人，iOS 主力）

**独享决策权（绝不委托给 Agent）**：

| 领域 | 具体事项 |
|---|---|
| 架构 | 技术选型、模块边界、语音链路方案、Go/No-Go 评审结论 |
| 体验红线 | 首响延迟、打断行为、反馈形式（徽章/动效取舍） |
| 质量仲裁 | 评估集标注争议的仲裁、Prompt 是否允许上线的最终签字 |
| 核心模块实现 | 音频引擎（AEC/打断）、SpeechSession 状态机——体验生死线必须亲手写 |
| 隐私与合规 | 数据删除链路、供应商条款确认、提审材料 |
| 商业化 | 定价、权益设计、V1.1 验证节点的判定 |

**委托给 Agent 但必须逐条评审**：全部 UI 页面实现、单测、文档更新。

### 2.2 后端工程支持（默认模式：Agent 起草 + 人写状态机，已定案）

后端不设专职人力，统一采用分层分工：

- **Agent 起草**：常规 CRUD 接口（account/material/session/corpus/drill/content）、语音网关的协议适配层与流转发、SQL 建表脚本；
- **负责人手写**：语音网关的会话状态机与打断后上下文一致性（技术方案坑清单 #4）、异步管线的事务/幂等关键路径——与音频引擎同级，属体验生死线；
- 人类对 Agent 起草部分的职责是**契约定义 + 代码评审**，不逐行实现。

---

## 三、AI Agent 分工矩阵

| Agent | 职责 | 主要产出 | 人类门禁 |
|---|---|---|---|
| **iOS 编程 Agent** | SwiftUI 页面、组件、SwiftData 缓存层实现；按 UI 文档还原；任务拆解与接口禁区见《FluentWork-iOS App端技术设计文档》第八章 | 页面代码 + 快照测试 | 负责人逐 PR 评审；音频与状态机相关代码一律人写 |
| **后端编程 Agent** | Go 业务接口（account/material/session/corpus/drill/content）、语音网关协议适配层与流转发起草、SQL 建表脚本 | 接口代码 + 集成测试 | 负责人评审；契约测试必须通过；网关状态机部分人写，Agent 不得改动 |
| **测试 Agent** | 单测生成（调度引擎状态迁移、额度校验、级联删除）、WSS 契约测试、压测脚本 | 测试代码与报告 | 覆盖率报告人工确认；测试本身抽查正确性（防"假测试"） |
| **标注 Agent** | 评估集样本预标注：按 2.2.4 维度给出初步结论 + 理由 | 预标注结果 | **人工 100% 复核**（评估集是裁判的基准，污染即全盘失效，此处不省人力） |
| **评估 Agent（LLM 裁判）** | 执行评估集回归、生成 diff 报告、线上抽检初筛 | 回归报告 | 报告结论由负责人签字后才允许 Prompt 灰度；裁判一致率 <85% 期间结论无效 |
| **内容 Agent** | 冷启动转录样本草稿、演示素材文案、推送/空态文案、每日读预置兜底内容 | 文本内容 | 负责人审读；涉英文表达的地道性由负责人（或英语母语顾问）确认 |
| **文档 Agent** | 文档间一致性同步（版本号、交叉引用）、会议纪要、质量周报汇总 | 文档 diff | 负责人抽查；文档变更走 git 评审 |
| **调研 Agent**（W1-W2 限时） | 火山引擎文档/论坛中端到端注入能力的信息收集，输出《端到端注入能力验证文档》的先验结论 | 调研报告 | 仅作先验参考，最终以实测 POC 为准 |

**关键纪律**：标注 Agent 与评估 Agent 的职责严格分离——预标注的 Agent 不能同时当裁判，否则评估体系自循环失效（对应 Prompt 文档 2.2.4"裁判不可自证"）。

---

## 四、工作流规范

### 4.1 任务下发格式（给任何编程 Agent）

每个任务单必须包含四要素，缺一不可：

1. **上下文文档链接**（本套文档的具体章节，而非口头描述）；
2. **接口契约**（输入输出、错误码）；
3. **验收标准**（可测试的条件列表）；
4. **禁区**（不许改动的模块、不许新增的依赖——对应技术方案"三方库白名单制"）。

### 4.2 合入门禁（CI 强制）

```
Agent 提交 → 编译 + SwiftLint/静态检查 → 单测全绿 → 契约测试通过
  → 人类评审（架构合规性 + 代码质量）→ 合入主干
```

- 调度引擎、额度校验、级联删除三类代码：Agent 可起草，但**单测必须由测试 Agent 独立生成后再交叉核对**（写代码的和写测试的不能是同一个 Agent 会话，防止自我盲区）；
- Prompt 变更：评估 Agent 回归 + 负责人签字双门禁（见 Prompt 文档 2.2.5）。

### 4.3 每日节奏（负责人）

| 时段 | 事项 |
|---|---|
| 上午 | 核心模块手写编码（音频引擎/状态机）；处理昨日 Agent 产出的评审队列 |
| 下午 | 下发新任务单；联调；评估集标注复核（内测期每日 ≤30 分钟） |
| 晚间（可选） | Agent 批处理任务（测试补全、文档同步）异步执行，次日晨检 |

---

## 五、周分工对齐（与 8 周排期一一对应）

| 周期 | 人类（负责人为主） | AI Agent |
|---|---|---|
| W1-W2 | 音频引擎 POC（亲手）；网关会话状态机手写；网关联调把关；注入能力实测；Go/No-Go 决策 | 后端编程：骨架工程/埋点 + 网关协议适配层起草；调研：火山注入能力先验报告；内容：冷启动转录样本 20 条起草 |
| W3-W4 | SpeechSession 状态机实现；Prompt 评估集仲裁；说的房间体验把关 | iOS：说的房间 UI/转录浮层；后端：account/material/session 接口；标注：冷启动评估集预标注；测试：WSS 契约测试 |
| W5 | 异步管线联调；评价/炼化 Prompt 调优签字 | 后端：ai-worker 管线；iOS：回顾页渐进式加载；评估：首版回归跑通 |
| W6 | 语料库体验把关；每日一读抽检 | 后端：批处理任务；iOS：语料库/每日一读页；内容：预置兜底读库 14 篇 |
| W7 | 调度参数评审；闪测交互打磨；周末提交 TestFlight 提审 | 后端：调度引擎单测（独立生成）；iOS：闪测卡流/结算页；测试：调度状态迁移全覆盖 |
| W8 | 内测支持；badcase 归档；延迟优化 | 测试：压测执行与报告；文档：内测手册；评估：内测真实样本替换自造样本（目标 50%） |

---

## 六、风险与对策

| 风险 | 对策 |
|---|---|
| Agent 产出质量波动（同一任务多次产出风格不一） | 任务单四要素强制；禁区清单；评审打回不修改直接重发任务单（不陷入与 Agent 的长对话拉扯） |
| 评估集被预标注偏见污染 | 预标注结果对复核人**隐藏 Agent 理由字段先行盲标**，再对比仲裁；每周抽 10% 双盲重标核对一致率 |
| 负责人单点故障（生病/阻塞） | 全部决策文档化；Agent 任务单即上下文，任何接手者可按任务单继续推进；每周文档 Agent 同步一次全库一致性 |
| 保密泄露（用户数据进入 Agent） | 保密边界纪律（第一章第 4 条）；内测数据只进自建评估管线，不出服务端 |
| Agent 生成的测试"看起来绿但断言空洞" | 测试代码评审时必查断言有效性；变异测试抽查（改一处实现看测试是否变红） |

---

## 七、效能口径（内测期开始统计）

1. **Agent 产出占比**：合入主干的代码中，Agent 起草行数占比（目标 ≥ 60%，低于此说明任务拆解粒度不够）；
2. **一次通过率**：Agent PR 无需返工直接合入的比例（目标 ≥ 50%，反映任务单质量）；
3. **评估集吞吐**：每周新增标注样本数（目标 ≥ 25 条/周，保证回归基准持续逼近真实分布）；
4. **人类核心时间占比**：负责人花在核心模块（音频/状态机/调参/仲裁）的时间占比（目标 ≥ 50%——这是本产品不可外包的部分）。

---

*评审重点：第二章不可委托清单、第三章标注与评估 Agent 的职责隔离、第四章合入门禁、第五章周对齐表。*
